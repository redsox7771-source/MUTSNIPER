services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mutsniper
      POSTGRES_PASSWORD: mutsniper
      POSTGRES_DB: mutsniper
    ports:
      - "5432:5432"
    volumes:
      - mutsniper-pgdata:/var/lib/postgresql/data

volumes:
  mutsniper-pgdata:
